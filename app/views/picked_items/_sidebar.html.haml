.span3
  .well.sidebar-nav
    %ul.nav.nav-list
      %li.nav-header Picked Items
      %li= link_to "Unpick all", clear_picked_path
      %li= link_to "Checkout", create_cart_path

      %li.divider

      %li.nav-header Hints
      .muted= auto_link("Unpick unwanted items or #{link_to("unpick all", clear_picked_path)} and start over")
      .muted= auto_link("Head back to #{link_to("search", search_path)} and add some more items")
      .muted= auto_link("#{link_to("Proceed", create_cart_path)} to Amazon when finished")
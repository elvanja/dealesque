// TODO see if there is a way to merge this with item partial in search, only form action is different

%li.span3
  .thumbnail
    %table.table-condensed
      %tr
        %td= image_tag item.images[:thumbnail].try(:url) || image_path("hot_plate.jpg")
        %td.span2
          %table.table-condensed
            %tr
              %td.span2= link_to item.title, item.url, {target: '_blank'}
            %tr
              %td= item.list_price.formatted
    .caption
      = form_tag unpick_item_path, method: :delete do
        = hidden_field_tag :item, item.extend(ItemRepresenter).to_json
        = submit_tag 'Unpick', class: "btn btn-warn btn-small"
